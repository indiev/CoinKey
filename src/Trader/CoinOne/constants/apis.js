
const COIN_ONE = 'https://api.coinone.co.kr/';
const ORDER_BOOK = `${COIN_ONE}orderbook`;
const TRADES = `${COIN_ONE}trades`;
const TICKER = `${COIN_ONE}ticker`;

export default {
  ORDER_BOOK,
  TRADES,
  TICKER
};
